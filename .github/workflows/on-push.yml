name: Deploy on GitHub pages
on: [push]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
       - name: Deploy 🚀
         uses: JamesIves/github-pages-deploy-action@v4
         with:
          folder: .